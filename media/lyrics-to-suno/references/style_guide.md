# Suno V5 Style 指南

- **采样规格**：V5 对 `High Fidelity` 和 `Nuanced Vocals` 有更好的理解。
- **结构标签**：建议使用 `[Drop]`, `[Bridge]`, `[Build]`, `[Outro]`。
- **情感词**：使用 `Vulnerability`, `Euphoric`, `Dark, gritty` 等。
- **冲突处理**：避免同时使用 `Fast` 和 `Ambient`。
